[project]
name = "hyperfocus"
version = "1.0.0"
description = "Minimal local MCP server for injecting narrative framing in LLM sessions"
readme = "README.md"
license = "BSD-3-Clause"
license_files = ["LICENSE"]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.26.0",
]
